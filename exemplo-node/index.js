const { batata, Churros } = require("./Churros");

//importar um conteúdo de fora      => import 
//passar para fora algum conteúdo   => export



const obj = {
    nome: "Victor",
    idade: 18
}

const { nome } = obj;